import { boot } from 'quasar/wrappers'
import axios from 'axios'

// Будет использоваться для работы с HTTP запросами при необходимости
// Сейчас используем fetch API напрямую в сервисах

export default boot(({ app }) => {
  // Конфигурация axios при необходимости
})

